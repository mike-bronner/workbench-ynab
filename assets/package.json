{
  "name": "@workbench-ynab/changeset",
  "private": true,
  "description": "Change-set schema, contract, validator, and example fixtures for workbench-ynab write-back (M4).",
  "main": "validate-changeset.js",
  "scripts": {
    "validate:fixtures": "node validate-changeset.js fixtures/categorize.example.json fixtures/allocate.example.json fixtures/delete-duplicate.example.json fixtures/reconcile.example.json fixtures/combined.example.json"
  },
  "dependencies": {
    "ajv": "8.17.1",
    "ajv-formats": "3.0.1"
  }
}
